/////////// TRIGGER
// Replace ? to whatever the maxspeed is
// Don't forget to adjust the trigger area

// Condition
this && (speed player) > ? && {_x iskindof "car"} count thislist > 0 && (vehicle player) in thislist && (driver vehicle player == player)
// On act
null = [[],"life_fnc_radarcontrollCity",player,false] spawn life_fnc_MP;

/////////// NPC (which will be like the radar in the real life)
this allowDamage false; this stop true; this disableAI "AUTOTARGET"; this disableAI "TARGET"; this disableAI "MOVE"; this disableAI "FSM"; removeAllWeapons this;this enableSimulation false;
/////////// Object
this allowDamage false; this enableSimulation false; this hideObject true;

